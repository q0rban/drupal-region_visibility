********************************************************************************
This module allows page specific visibility settings, similar to how the 
visibility settings work when configuring a block.  To install with the module,
simply enable, configure permissions, and then visit the theme settings pages at
admin/settings/region_visibility to configure the regions for each theme.